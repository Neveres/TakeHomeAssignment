import { css } from '@emotion/react'

export const reviewContainer = css`
  * {
    font-weight: 500;
    font-size: 16px;
  }

  .review-header-container {
    margin-bottom: 15px;
    .header {
      margin-bottom: 7px;
      font-weight: 700;
    }
  }

  .see-more-reviews {
    display: block;
    text-align: center;
    cursor: pointer;
    font-size: 12px;
    font-weight: 700;
  }
`
